@model Ropey_DvDs_Group_CW.Models.StudioModel

@{
    ViewData["Title"] = "Edit";
}


<div class="row">
    <div class="col-md-4 mx-auto card mt-5 pt-4 p-5">
        <h4>Edit Studio</h4>
            <hr />
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="StudioNumber" />
            <div class="form-group">
                <label asp-for="StudioName" class="control-label">Studio Name</label>
                <input asp-for="StudioName" class="form-control" />
                <span asp-validation-for="StudioName" class="text-danger"></span>
            </div>
            <div class="form-group mt-3">
                <input type="submit" value="Save" class="btn btn-primary btn-danger" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
